TODO:

- cleanup code
- convert current implementation to mode-based ~~ almost done
- add comments to code ~~ logging system in place but still needs work
x fix the automatic color adder ~ fixed
- cleanup "internal_api.h"
- add features and more modes 